# 实施计划：本地解析失败防护与诊断

1. 读取 backend 与 Web 相关规范，确认 `ParsedDocument`、`ExpenseRecord.raw_result`、任务文件行和预览的现有契约。
2. 在快速路径、Paddle 路径及 MinerU 路径写入统一、脱敏的解析轨迹和原因代码；不记录密钥或原始服务响应。
3. 在流水线将云端兜底成功转为非阻塞 `risk_note`，全部失败保留可行动的错误摘要。
4. 在 Web 预览/任务文件状态中展示解析来源与原因；提供按来源和失败代码汇总的诊断信息，并确保重试会刷新结果。
5. 添加单元与 Web 测试：本地成功、本地失败后 Paddle 成功、Paddle 后 MinerU 成功、全部失败、重试刷新与敏感信息不泄漏。
6. 运行相关 pytest、`git diff --check`，并使用真实样票包验证提示不影响报销汇总与导出。

## 回滚

解析轨迹和展示为附加元数据；如出现回归，可撤回该元数据写入与 UI 展示，而不会改变既有 OCR、金额或导出结果。
